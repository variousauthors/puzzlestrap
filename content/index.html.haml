---
---

.layers
  %ul#layer-select
    %li#background.tab.active{ data: { "layer-name" => "layer-background" } }
      Background
    %li#create-layer.tab
      +
  %ul#layers
    %li.active{ data: { "layer-name" => "layer-background" } }
      .preview
      .name
        Background

      %input#files{ type: "file", name: "files[]", multiple: true }

.tiles

%canvas#canvas{ width: 400, height: 300 }

%pre#puzzlescript{ style: "border: 1px solid red;" }

%script#layer-template{ type: "text/template" }
  %li
    .preview
    .name
      NEW LAYER

    %input#files{ type: "file", name: "files[]", multiple: true }

%script#layer-tab-template{ type: "text/template" }
  %li.tab
    New Tab


